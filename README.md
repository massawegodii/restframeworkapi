# Django REST Framework CRUD API

This is a simple CRUD API built with **Django** and **Django REST Framework**.

## Features

- Create, Read, Update, Delete (CRUD) operations for Products
- JSON API endpoints
- Built with Django REST Framework
- Ready to connect to frontend apps (Flutter, React, etc.)

## Requirements

- Python 3.9+
- Django
- Django REST Framework
- Virtual environment recommended

